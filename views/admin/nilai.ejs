<!DOCTYPE html>
<html lang="id">
<head>
    <title>Input Nilai Siswa</title>
    <%- include('../partials/head') %>
</head>
<body>
    <div class="app-layout">
        <%- include('../partials/sidebar', { user: user, activePage: activePage }) %>
        <div class="main-content">
            <%- include('../partials/topbar', { user: user, pageTitle: pageTitle }) %>
            <div class="content-wrapper">
                <div class="panel" style="background:#f4f8ff;">
                    <div class="panel-header-row" style="cursor:pointer;" onclick="document.getElementById('tutorialNilai').classList.toggle('show')">
                        <div class="panel-header">
                            <div class="stat-icon icon-blue" style="width:36px;height:36px;font-size:16px;"><i class="bi bi-question-circle-fill"></i></div>
                            <h3>Cara Mengisi Nilai Siswa</h3>
                        </div>
                        <i class="bi bi-chevron-down"></i>
                    </div>
                    <div id="tutorialNilai" class="tutorial-content">
                        <ol style="padding-left:20px; font-size:14px; color:#333; line-height:1.8;">
                            <li>Klik <strong>"Unduh Template"</strong> — nilai yang sudah pernah diisi akan otomatis muncul di template.</li>
                            <li>Isi kolom nilai sesuai NISN (kolom NISN berwarna kuning, jangan diubah urutannya).</li>
                            <li>Klik <strong>"Import Nilai (Excel)"</strong> dan unggah file yang sudah diisi.</li>
                            <li>Atau klik ikon pensil di tabel siswa untuk isi nilai satu per satu secara manual.</li>
                        </ol>
                    </div>
                </div>
                <% if (success) { %>
                    <div class="alert alert-success"><i class="bi bi-check-circle-fill"></i> <%= success %></div>
                <% } %>
                <% if (error) { %>
                    <div class="alert alert-danger"><i class="bi bi-exclamation-circle-fill"></i> <%= error %></div>
                <% } %>
                <% 
                    const belumLengkap = siswaList.filter(s => s.jumlah_terisi < s.total_kriteria);
                %>
                <% if (belumLengkap.length > 0) { %>
                <div class="panel" style="border-left:4px solid #e67e22;">
                    <div class="panel-header">
                        <div class="stat-icon icon-orange" style="width:36px;height:36px;font-size:16px;"><i class="bi bi-exclamation-triangle-fill"></i></div>
                        <h3>Siswa Belum Lengkap Nilai (<%= belumLengkap.length %>)</h3>
                    </div>
                    <table class="data-table">
                        <thead><tr><th>NISN</th><th>Nama</th><th>Kelengkapan</th><th>Aksi</th></tr></thead>
                        <tbody>
                            <% belumLengkap.forEach(function(s) { %>
                                <tr>
                                    <td><%= s.nisn %></td>
                                    <td><%= s.nama %></td>
                                    <td><span class="badge badge-mengundurkan_diri"><%= s.jumlah_terisi %> / <%= s.total_kriteria %></span></td>
                                    <td><a href="/admin/nilai/<%= s.id_siswa %>" class="btn-icon" title="Lengkapi"><i class="bi bi-pencil-square"></i></a></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
                <% } %>
                <div class="panel">
                    <div class="panel-header-row">
                        <div class="panel-header">
                            <div class="stat-icon icon-blue" style="width:36px;height:36px;font-size:16px;"><i class="bi bi-pencil-square"></i></div>
                            <h3>Input Nilai Siswa per Kriteria</h3>
                        </div>
                        <div style="display:flex; gap:10px;">
                            <a href="/admin/nilai/unduh-pdf" class="btn-secondary" style="text-decoration:none; display:flex; align-items:center; gap:6px;">
                                <i class="bi bi-file-earmark-pdf-fill"></i> Unduh PDF Nilai
                            </a>
                            <a href="/admin/nilai/template" class="btn-secondary" style="text-decoration:none; display:flex; align-items:center; gap:6px;">
                                <i class="bi bi-download"></i> Unduh Template
                            </a>
                            <button class="btn-primary" onclick="document.getElementById('modalImportNilai').style.display='flex'">
                                <i class="bi bi-file-earmark-excel-fill"></i> Import Nilai (Excel)
                            </button>
                        </div>
                    </div>
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>No</th>
                                <th>NISN</th>
                                <th>Nama</th>
                                <th>Kelengkapan Nilai</th>
                                <th>Aksi</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (siswaList.length === 0) { %>
                                <tr><td colspan="5" class="empty-state">Belum ada data siswa. Tambahkan siswa terlebih dahulu di menu Data Siswa.</td></tr>
                            <% } %>
                            <% siswaList.forEach(function(s, index) { %>
                                <tr>
                                    <td><%= index + 1 %></td>
                                    <td><%= s.nisn %></td>
                                    <td><%= s.nama %></td>
                                    <td>
                                        <span class="badge badge-<%= s.jumlah_terisi >= s.total_kriteria && s.total_kriteria > 0 ? 'green' : 'red' %>">
                                            <%= s.jumlah_terisi %> / <%= s.total_kriteria %> kriteria
                                        </span>
                                    </td>
                                    <td>
                                        <a href="/admin/nilai/<%= s.id_siswa %>" class="btn-icon" title="Isi Nilai">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
                <%- include('../partials/footer') %>
            </div>
        </div>
    </div>
    <!-- Modal Import Nilai -->
    <div class="modal-overlay" id="modalImportNilai">
        <div class="modal-box">
            <div class="modal-header">
                <h3><i class="bi bi-file-earmark-excel-fill"></i> Import Nilai dari Excel</h3>
                <button class="modal-close" onclick="document.getElementById('modalImportNilai').style.display='none'">&times;</button>
            </div>
            <form action="/admin/nilai/import" method="POST" enctype="multipart/form-data">
                <p style="font-size:13px; color:#777; margin-bottom:15px;">
                    Nilai akan dicocokkan berdasarkan kolom <strong>NISN</strong>. Unduh template terlebih dahulu agar format kolom sesuai.
                </p>
                <label>Pilih File Excel (.xlsx / .xls)</label>
                <input type="file" name="fileExcel" accept=".xlsx,.xls" required>
                <div class="modal-footer">
                    <button type="button" class="btn-secondary" onclick="document.getElementById('modalImportNilai').style.display='none'">Batal</button>
                    <button type="submit" class="btn-primary">Import Sekarang</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>